<div align="center">
  <img src="https://2z2.org/upload/jm-aura-nobg.png" alt="JM-Aura" width="250" height="200"/>

  <h1><i>JM-Aura</i></h1>
  <p><i>一个简洁、优雅的 JMComic 漫画阅读/下载 Web 应用</i></p>

  [![GitHub](https://img.shields.io/badge/-GitHub-181717?logo=github)](https://github.com/Tom6814)
  [![GitHub license](https://img.shields.io/github/license/Tom6814/JM-Aura)](https://github.com/Tom6814/JM-Aura/blob/master/LICENSE)
  [![Python Version](https://img.shields.io/badge/python-3.10+-blue?logo=python)](https://www.python.org/)
</div>

---

# 项目简介

**JM-Aura** 是一个面向 **JMComic** 的漫画阅读与下载工具。它采用 **FastAPI** 作为后端，**Vue3 (CDN)** 作为前端，提供了一个现代化、响应式的 Web 界面。

无需复杂的客户端安装，部署在服务器或本地后，通过浏览器即可享受流畅的漫画阅读体验。

## ✨ 核心优势

*   **开箱即用**：无需复杂客户端安装，浏览器打开就能用；前端无构建步骤，部署更省心。
*   **极致阅读**：
    *   专为长条漫优化的垂直滚动模式。
    *   **自动去码**：内置 Canvas 自动处理图片去码逻辑。
    *   **智能预加载**：按阅读顺序优先加载图片，省流且流畅。
    *   自定义图片宽度与间距，适配各种屏幕尺寸。
*   **下载自由**：
    *   **批量下载**：支持多选章节，后台队列下载。
    *   **自动打包**：下载完成后自动打包为 ZIP，并清理临时文件，节省空间。
*   **网络无忧**：
    *   内置 **图片代理**，自动处理防盗链和跨域问题。
    *   支持多线路切换，自动应对 API 屏蔽。
*   **个性化主题**：
    *   内置 **粉色 (默认)**、**橙色**、**绿色**、**优香蓝 (Yuuka Blue)** 等多套 Material Design 主题。
    *   支持深色/浅色模式切换。

## 🚀 快速开始 (小白向部署教程)

以下教程以 **Windows** 和 **Linux (Ubuntu/Debian)** 为例。

### 前置准备

确保你的电脑/服务器已安装 **Python 3.10** 或更高版本。

### Windows 部署

1.  **下载代码**：点击右上角 `Code` -> `Download ZIP` 下载并解压。
2.  **进入目录**：在解压后的文件夹内，右键 -> `在终端中打开`。
3.  **安装依赖**：
    ```powershell
    # 创建虚拟环境 (可选，但推荐)
    python -m venv .venv
    # 激活虚拟环境
    .\.venv\Scripts\activate
    
    # 安装依赖库
    pip install -r requirements.txt
    ```
4.  **启动服务**：
    ```powershell
    # 启动后端服务
    python -m backend.main
    ```
5.  **访问**：打开浏览器访问 `http://127.0.0.1:8000` 即可使用。

### Linux 服务器部署 (推荐)

适合想要 24 小时挂机下载或远程阅读的用户。

1.  **安装基础工具**：
    ```bash
    sudo apt update
    sudo apt install -y python3 python3-venv python3-pip git
    ```

2.  **下载并安装**：
    ```bash
    # 创建目录
    sudo mkdir -p /opt/jm-aura && sudo chown -R $USER:$USER /opt/jm-aura
    cd /opt/jm-aura
    
    # 放置代码 (可以使用 git clone 或上传 zip)
    # git clone https://github.com/Tom6814/JM-Aura.git .
    
    # 安装依赖
    python3 -m venv .venv
    .venv/bin/pip install -r requirements.txt
    ```

3.  **后台运行 (Systemd)**：
    为了让它关掉终端也能跑，我们把它注册成服务。
    
    ```bash
    # 创建服务文件
    sudo nano /etc/systemd/system/jm-aura.service
    ```
    
    粘贴以下内容：
    ```ini
    [Unit]
    Description=JM-Aura Web
    After=network.target
    
    [Service]
    Type=simple
    WorkingDirectory=/opt/jm-aura
    # 注意：这里的路径要和你实际安装路径一致
    ExecStart=/opt/jm-aura/.venv/bin/python -m backend.main
    Restart=always
    
    [Install]
    WantedBy=multi-user.target
    ```
    
    保存退出 (Ctrl+O, Enter, Ctrl+X)。然后启动：
    ```bash
    sudo systemctl enable --now jm-aura
    ```

4.  **访问**：浏览器访问 `http://<服务器IP>:8000`。

## ⚙️ 配置说明

*   **账号登录**：首次访问请点击右上角 `Settings`，在 Account 栏目中输入 JMComic 的账号密码。
*   **配置文件**：
    *   `config/op.yml`：自动生成的配置文件，包含 API 线路等信息。
    *   `backend/config/cookies.json`：保存登录后的 Cookie 信息。
    *   **注意**：这两个文件包含敏感信息，请勿分享给他人。

## 🛠️ 常见问题

**Q: 图片加载不出来？**
A: 请在 `Settings` 中尝试切换 API 线路，或检查服务器是否能访问外网。JM-Aura 内置了图片代理，通常能解决大部分加载问题。

**Q: 评论发不出去？**
A: 上游站点有防刷屏机制，请避免发送过短或重复的内容。

**Q: 如何更新？**
A: 
```bash
cd /opt/jm-aura
git pull
.venv/bin/pip install -r requirements.txt
sudo systemctl restart jm-aura
```

## 🤝 鸣谢

本项目参考/借鉴了以下开源项目：
- [tonquer/JMComic-qt](https://github.com/tonquer/JMComic-qt)

- [hect0x7/JMComic-Crawler-Python](https://github.com/hect0x7/JMComic-Crawler-Python)

## ⚠️ 免责声明

本项目仅供学习交流使用，请勿用于商业用途。使用者应遵守当地法律法规及目标网站的使用条款。开发者不对使用本项目产生的任何后果负责。
